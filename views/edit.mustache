<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link type= "text/css" rel="stylesheet" href="/css/styles.css">
    <title>Poker Games</title>
  </head>
  <body>
    <h1 id="editHeader">Edit a Poker Game</h1>

    <div class="formDiv">
      <form class="inputInfo" action="/games/{{game._id}}" method="post">

        <div class="gameName">
          <label for="gameName">Name of Game:</label>
          <input type="text" name="gameName" value="{{game.game}}">{{#errors.game}}Name is required{{/errors.game}}
        </div>
        <div class="playedIn">
          <label for="">Location Most Played In:</label>
          <input type="text" name="playedIn" value="{{game.mostlyPlayedIn}}">
        </div>
        <div class="cardsDealt">
          <label for="">Number of Cards Dealt to Player:</label>
          <input type="number" name="cardsDealt" value="{{game.cardsDealtToPlayer}}">{{#errors.cardsDealtToPlayer}}Please enter the number of cards dealt to the player{{/errors.cardsDealtToPlayer}}
        </div>

        <button id="editButton" type="submit" name="submitButton" value="">Edit Game</button>

      </form>

    </div>
  </body>
</html>
